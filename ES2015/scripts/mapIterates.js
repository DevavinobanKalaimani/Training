let map = new Map([
    ['fname', 'Deva'],
    ['lname', 'K']
]);
for (let key of map.keys()) {
    console.log(key);
}
for (let [key, value] of map.entries()) {
    console.log(`${key} -> ${value}`);
}
//# sourceMappingURL=mapIterates.js.map